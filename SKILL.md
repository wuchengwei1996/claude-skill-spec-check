---
name: spec-check
description: Use when 用戶要求檢查、驗證、體檢一份 spec 或設計文件——說「/spec-check」「檢查這份 spec」「spec 體檢」「驗一下 spec」「對 spec 做 premortem」時觸發。適用於 spec／需求文件定稿前的完整性稽核與風險檢查；不用於程式碼審查、專案該不該做的可行性／ROI 評估、或產 implementation plan——這三件事各有工具（若裝了 superpowers，plan 走 superpowers:writing-plans）。
---

# spec-check：spec 完整性稽核＋多面向 premortem

指揮官編排、agents 實查、裁決留人。產出＝修改計畫（落檔），**不直接改 spec**。
交辦範本→本檔附錄 A；修改計畫格式→附錄 B。

## 硬性約束（違反即流程失敗）

- 指揮官不下場逐行實查；agents 只讀不寫。
- **已定決策不重審**：context-pack 列明「已定」的項目，agents 不得提案推翻；除非附新事實證據，單獨標「重啟裁決請求」列出，不混入一般 findings。
- **每條建議必答「不修，會發生什麼具體失敗」**——答不出的降 🟢 或刪。（實測 baseline：11 條建議全是加項、零砍項；premortem 視角天生偏向加東西，這條是唯一反制。）
- 修改計畫排最前的是 🔴 需用戶裁決項；用戶核准前不執行任何 spec 修改。

## 流程

### 1. 素材盤點 → context-pack 落檔

讀 spec 本體；盤點：對話中已拍板的決策、**spec 裡有寫但尚未經用戶核准的段落**、待定項、用戶提供的資料路徑、相關的既有決策紀錄（若有）。
落檔 spec 同目錄 `YYYY-MM-DD-<spec名>-稽核上下文.md`（`<spec名>`＝spec 檔名主幹；主幹已含日期則不重複加）：

- 已定決策（各附出處：對話日期／紀錄檔名）
- 待定項與未核准段落（實測 baseline 漏抓這類，因為只存在對話層）
- 素材清單＋路徑（agents 據此讀，不各自盲搜——實測單 agent 自行重讀全部素材 ~107K tokens，共用預消化是主要省錢點）
- 本輪檢查範圍邊界
- 檔頭標註：「快照，攝於 <日期>；決策長期真值源＝spec 決策點節，修改計畫執行時應吸收本檔內容後封存」

### 2. 單一確認點（一則訊息給齊，只等用戶一次）

- 素材清單：「有沒有我漏掉的資料？」
- **現況圖說**（ASCII）：選型判準＝**哪種圖最能證明你讀懂全文、且呈現 spec 現況**——系統類常是架構圖、流程類是流程圖、內容類是關係圖、其他用章節結構樹＋每節一句話；spec 本身已有架構圖時**不要原樣照抄**（無鑑別力），畫它沒有的視角。目的：用戶 10 秒驗證指揮官有沒有讀錯 spec，讀錯就會派錯工。
- 面向選單：從【技術可行性／使用情境／前端／後端／資料與整合／維運／成本時程】依 spec 性質勾 1〜3 個，**外加必選「過度工程（砍項）」面向**（專找該刪的，不找該加的）。
- 用戶可說「直接跑」跳過等待，事後在修改計畫裡糾正。

### 3. 並行派工（建議模型路由，依你的環境調整）

| 角色 | 數量 | 模型 | 基底 |
|---|---|---|---|
| 完整性審查 | 1 | opus | 附錄 A-1：內部矛盾／模糊句／驗收判準盲區／素材對照（context-pack 有、spec 沒吸收的） |
| premortem 各面向 | 1〜3 | sonnet | 附錄 A-2：「假設照這份 spec 做完後專案失敗了，從你的面向解釋為什麼」 |
| 過度工程（砍項） | 1 | sonnet | 附錄 A-2 反向：「哪些內容第一版不需要？刪掉會怎樣？」 |

每個 prompt 必含：context-pack 路徑（必讀）＋「每條 finding 引 spec 章節與行號；禁通用最佳實踐建議；最多 5 條，依嚴重度排序」＋回報格式與行數上限。
規模判斷：spec < 200 行 → 完整性＋砍項＋1 個面向共 3 個 agents 就夠；面向數上限 5 個 agents。

### 4. 彙整裁決（指揮官親做，不外包）

併排去重 → 抽驗：**🔴 級全數查證據、🟡🟢 至少抽 1** → 分級錨定「不修的具體失敗情境」→ 過度工程過濾：每條「建議新增」逐條問「第一版不加會怎樣」。

### 5. 修改計畫落檔

spec 同目錄 `YYYY-MM-DD-<spec名>-修改計畫.md`，照附錄 B：

- 開頭放現況全貌圖（步驟 2 定稿版）
- 每個 🔴🟡 項附 **before → after 同框**：架構有變畫圖、純文字修改用對照表
- 🔴 需裁決項排最前；文中引用 context-pack 為稽核軌跡
- 🔴 ≥ 3 項 → 另產「裁決用摘要」：每項一張 before→after 圖說＋一句「不修會怎樣」，Markdown 即可；若做成 HTML 簡報，須本地開檔確認無內容被裁切

用戶核准後的修改執行＝另行流程（同 session 可做：逐項改 spec → read-back 驗證 → 吸收並封存 context-pack）。

## 常見失誤（2026-08-27 baseline 實測）

| 失誤 | 對策位置 |
|---|---|
| 加項偏誤：建議全是補／加，零砍項 | 硬性約束第 3 條＋必選砍項面向 |
| 漏抓「spec 有寫但未經用戶核准」段落 | 步驟 1 未核准段落清單 |
| 重啟已定決策 | 硬性約束第 2 條 |
| findings 平鋪無分級，用戶自己 triage | 步驟 4 分級＋🔴 排前 |
| 純文字牆，架構與差異不可見 | 步驟 2 圖說＋步驟 5 before/after |
| 每個 agent 各自重讀全部素材，成本爆量 | 步驟 1 context-pack 預消化共用 |

## 附錄 A：交辦範本（複製後填【】）

### A-1 完整性審查（fresh context）

```
你是對抗審查員（fresh context，與產出者無關）。審查對象：【spec 路徑】；必讀：【context-pack 路徑】。
立場：先假設它有錯，設法證明；找不到問題也要說明你檢查了哪些面向。
審查維度：內部矛盾／模糊句（兩種讀法會導致不同行為）／驗收條件是否可機械檢查／
路徑與工具名是否真實存在（逐一驗證）／素材對照（context-pack 有、spec 沒吸收的）／安全與邊界。
只讀取＋驗證，禁止修改任何檔案；發現的問題不要動手修，回報就好。
context-pack 標「已定」的決策不得提案推翻；有新事實證據才單獨列「重啟裁決請求」。
驗收條件：
- 每個 finding：位置（章節＋行號）＋問題描述＋不修會發生什麼具體失敗＋建議修法
- 分級：🔴 必修（會造成錯誤行為）／🟡 建議（易誤讀）／🟢 備註
- 引用的路徑/工具名/命令你必須實際驗證過存在與否（ls/grep 為證）
- 最多 5 條，依嚴重度排序；禁通用最佳實踐建議
回報格式（≤50 行）：🔴 清單 → 🟡 清單 → 🟢 清單 → ## 已檢查但無問題的面向
```

### A-2 premortem 視角（每個面向一份；砍項面向把問題句換成反向）

```
你是【面向：如 技術可行性／使用情境／資料與整合／維運／成本時程】的獨立分析者。
你不做最終決定，只從這個面向提供最強素材給決策者；不要臆測其他面向怎麼看。
問題：假設照這份 spec 做完後專案失敗了，從你的面向解釋為什麼。
　　（砍項面向改問：哪些內容第一版不需要？刪掉會怎樣？）
必讀：【context-pack 路徑】；審查對象：【spec 路徑】。
只讀取與分析，禁止修改任何檔案；context-pack 標「已定」的決策不得提案推翻。
驗收條件：
- 每條 finding 引 spec 章節與行號，並答「不修，會發生什麼具體失敗」——答不出的不要列
- 最多 5 條，依嚴重度排序；禁通用最佳實踐建議
- 一句話：你面向下唯一必守的底線
回報格式（≤25 行）：## findings（≤5 條） ## 必守底線（1 句）
```

指揮官彙整（不可外包）：併排 N 份 → 標分歧點與共識點 → 對每個分歧開章節：行號抽驗哪方證據更硬 → 裁決留指揮官／🔴 級回問用戶；禁讓 agent 投票定案。

## 附錄 B：修改計畫格式

- 人類層在上、機器層在分隔線「──────  以下給 agent 執行，你可以不看  ──────」之下。
- 人類層必備節：## 這是什麼（一句話）／## 為什麼改・明確不改／## 決策點（含前提假設）／## 名詞對照；每節第一句粗體給結論。
- 開頭兩行對比：😣 現在：<痛> ／ 🎯 之後：<改完的樣子>。
- 英文保留＋中文並陳，行內只給短中文（如 CLI（命令列工具）），英文全稱只放文末名詞對照。
- 圖用 ASCII／表格，不用 mermaid；人類層禁裸用內部代號（M1／L2 那種）。
- 選配（該用才用）：全貌 ASCII 圖＋你在這裡、🧩 卡關點、🔴 需要你決定。（本 skill 的修改計畫**開頭必放**步驟 2 定稿的現況全貌圖，不算選配。）
- 機器層：## Tasks（編號、標依賴、機械可驗）／## 驗收。
